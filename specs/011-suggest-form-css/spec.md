# Feature Specification: Suggest-Feature Form Button Layout

> **Status: Archived (frozen 2026-06-09)**

Equalized the Cancel and Send buttons in the suggest-a-feature form so they share the action row evenly. Achieved with a `btn--half` modifier plus `flex: 1 1 0`, giving both buttons identical width. The form's footer now reads as a balanced, symmetric pair.

**Merged**: PR #12.

See the `CLAUDE.md` prior-features summary and git history (`git log -- specs/011-suggest-form-css/`) for full detail.
