# Feature Specification: UI Copy Refinements — Header Intro, Score Formula, "Point" Relabel

> **Status: Archived (frozen 2026-06-09)**

This feature delivered a set of UI copy refinements: a short explanatory intro added to the header, a caption clarifying the score formula, and a relabel of the "note" terminology to "point" across the interface. The changes were cosmetic copy adjustments that improved clarity without altering any behaviour or data. The outcome shipped as planned with no follow-up issues.

**Merged**: PR #6.

See the `CLAUDE.md` prior-features summary and git history (`git log -- specs/005-ui-copy-refinements/`) for full detail.
